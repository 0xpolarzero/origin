# 14 Release Gate Report

Timestamp (UTC): 2026-03-06T20:41:48Z

Decision: `GO`

Go/No-Go Owner: `Origin desktop Phase 14 release owner`

Open-Defect Owner: `Origin app/opencode maintainers`

## Release-Blocking Severity Rubric

- `critical`: duplicate external side effect, policy bypass, secret leakage, destructive corruption, or unrecoverable data loss.
- `high`: wrong terminal state, missing dispatch/audit provenance, broken approval/send safety gate, or failed recovery path in a release-blocking scenario.

## Release-Blocking Suite Boundary

Phase 14 treats "named automated e2e" as named automated assertions executed inside the local release harness. For this gate, that harness includes the browser UI slice (`packages/app` Playwright), the local server-route/runtime/integration slice (`packages/opencode` tests executing the real local state-machine and persistence stack), and the security canary slice. The matrix `scenario_class` column records which slice closes each invariant.

Phase 14 blocks release on the following zero-skip suites:

- App/browser blocking slice:
  - `bun run --cwd packages/app test:e2e:local -- e2e/drafts/drafts.spec.ts e2e/release-gate/release-gate.spec.ts e2e/security/outbound-governance.spec.ts e2e/notifications/debug-reconciliation.spec.ts e2e/history/history.spec.ts e2e/scheduler/cron-history.spec.ts e2e/triggers/signal-history.spec.ts`
  - Outcome: `22 passed`, `0 skipped`, `0 failed`.
- Runtime/integration blocking slice:
  - `bun test --timeout 30000 test/runtime/outbound.test.ts test/workflow/integration-queue.test.ts test/workflow/debug-report.test.ts test/workflow/manual-run.test.ts test/server/workflow-routes.test.ts test/server/workflow-history-routes.test.ts test/workflow/trigger-engine.test.ts test/runtime/trigger-runtime.test.ts test/logging/redaction.test.ts` (from `packages/opencode`)
  - Outcome: `84 passed`, `0 skipped`, `0 failed`.

The broader verification commands required by `AC-07` remain mandatory but are not the release-blocking suite boundary for `AC-05`, because they currently include unrelated legacy skips outside the Phase 14 scenario set.

## Flake Policy Outcome

- Policy for this phase: every release-blocking suite gets one immediate rerun after a failure; pass-on-rerun still blocks release until fixed or explicitly removed from the release-blocking set by spec delta; skipped/quarantined tests are not allowed in the release-blocking suite.
- Outcome for final gate:
  - No release-blocking suite was accepted on pass-on-rerun alone.
  - Failures encountered during implementation were deterministic product or test-harness defects that were fixed in code before the final gate.
  - Final app/browser blocking slice passed on the recorded run above with `22 passed`, `0 skipped`, `0 failed`.
  - Final runtime/integration blocking slice passed on the recorded run above with `84 passed`, `0 skipped`, `0 failed`.
  - `packages/app/script/e2e-local.ts` now defaults Playwright to `--workers 1` unless explicitly overridden, which removes cross-test worker pressure from the local release harness.

## Matrix Audit

- The committed matrix is the exhaustive critical PRD invariant register for this phase gate, distilled from the PRD baseline plus the Phase `04` through `13` baselines cited by the spec.
- `docs/specs/14.e2e-matrix.md` contains `20` release-trace rows.
- Every listed Phase 14 release-blocking scenario has a non-empty `scenario_id` and `test_id`.
- No critical invariant row has an empty `test_id`.

## Validation Results

- PASS `bun run --cwd packages/opencode typecheck`
- PASS `bun run --cwd packages/opencode test` with `1381 pass`, `5 skip`, `0 fail`
- PASS `bun run --cwd packages/app typecheck`
- PASS `bun run --cwd packages/app test:unit` with `324 pass`, `0 fail`
- PASS `bun run --cwd packages/app test:e2e:local -- e2e` with `140 pass`, `3 skip`, `0 fail`
- PASS `bun run --cwd packages/desktop typecheck`
- PASS `bun ./packages/sdk/js/script/build.ts`
- PASS `git diff --exit-code -- packages/sdk/js/src/gen packages/sdk/js/src/v2`

Supporting no-skip release-blocking evidence:

- PASS app/browser blocking slice with `22 passed`, `0 skipped`, `0 failed`
- PASS runtime/integration blocking slice with `84 passed`, `0 skipped`, `0 failed`
- PASS `bun test --cwd packages/opencode test/session/session.test.ts`
- PASS `bun test test/workflow/manual-run.test.ts` (from `packages/opencode`) with `14 passed`, `0 failed`
- PASS `bun test --cwd packages/app src/pages/history-drafts.test.ts`

## Defect Triage

- Unresolved `critical`: `0`
- Unresolved `high`: `0`
- Unresolved `medium`: `0` release-blocking
- Unresolved `low`: `0` release-blocking

Residual non-blocking observations:

- `bun run --cwd packages/opencode test` still reports `5` skips outside the Phase 14 release-blocking suite boundary:
  - unicode filenames modification and restore
  - four structured-output integration cases
- `bun run --cwd packages/app test:e2e:local -- e2e` still reports `3` skips outside the Phase 14 release-blocking suite boundary.
- Full app e2e runs still emit expected negative-path server errors and cleanup noise (`NotFoundError`, `Impossible`, `WorktreeResetFailedError`, and missing-file logs) while exiting green. These logs did not correspond to failed assertions in the release-blocking scenarios.

## Ownership

- Blocking defect owner: `Origin desktop Phase 14 release owner`
- Runtime/backend follow-up owner for non-blocking residual suite noise: `Origin opencode maintainers`
- App/harness follow-up owner for non-blocking residual suite noise: `Origin app maintainers`

## Gate Outcome

Release gate outcome is `GO` on `2026-03-06T20:41:48Z`.

The committed matrix, targeted zero-skip release-blocking evidence, and required command suite together satisfy `AC-01` through `AC-07` for Phase 14 without requiring a spec delta.
