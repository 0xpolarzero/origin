You are working in `/Users/polarzero/code/projects/origin`.

Your task is to produce a new Phase 16 spec that would implement and test all missing UI in this fork, so that the app ends up with a thorough, comprehensive, fully integrated UI layer and strong local e2e coverage.

Important: do not rely on previous summaries as complete. Re-derive the truth from the repo.

Process requirements:
- Read `/Users/polarzero/code/projects/origin/docs/specs/00.project-direction.md` first.
- Then read `/Users/polarzero/code/projects/origin/docs/specs/GUIDE.md`.
- Follow the required `.spec` / `.log` workflow from `GUIDE.md`.
- Treat earlier “out of scope” / “deferred” UI items as candidates for Phase 16. Do not exclude them just because earlier phases postponed them.
- Use subagents heavily to preserve context and parallelize the audit. Spawn multiple explorer subagents in parallel and keep each one narrowly scoped.
- Ask me a concise question only if you hit a real product ambiguity with materially different UX outcomes. Otherwise proceed on your own and make sensible UX/UI choices.

Subagent expectations:
- Use subagents aggressively.
- At minimum, split the work into parallel sweeps for:
  1. Specs and product-intent audit
  2. Current app implementation audit
  3. Existing test and e2e coverage audit
  4. Deferred/future UI items already approved in specs/drafts
- Reuse subagents where it makes sense.
- Keep your own local context small by letting subagents carry detail.

Sources you should review:
- `/Users/polarzero/code/projects/origin/docs/specs/00.project-direction.md`
- `/Users/polarzero/code/projects/origin/docs/specs/GUIDE.md`
- All phase specs in `/Users/polarzero/code/projects/origin/docs/specs`
- Product-intent drafts when useful, especially:
  - `/Users/polarzero/code/projects/origin/docs/drafts/baseline.prd.draft.md`
  - `/Users/polarzero/code/projects/origin/docs/drafts/graph-first-workflows-and-runtime.design.draft.md`
- Current app code in `/Users/polarzero/code/projects/origin/packages/app`

Before writing the spec, build an exhaustive inventory of UI that is still missing or too thin. This must include at least:
- Workflow discovery/list/detail/run surfaces
- Manual run start, input collection, rerun flows
- Workflow authoring, editing, AI builder, graph editing, node editing, edit history/checkpoints if they are part of the approved direction
- Library browsing, detail, used-by, history, editing, copy-on-write flows
- App shell navigation and discoverability
- History, runs, operations, drafts cross-links and navigation consistency
- Debug/reconciliation UI, reporting flow, and settings
- Trigger/scheduler visibility UI where needed for a coherent product
- Notification and deep-link behavior
- Session-role UX for execution sessions, follow-up sessions, builder sessions, node-edit sessions
- Accessibility and keyboard behavior
- Copy, labeling, data presentation, and empty/loading/error/blocked states
- Local e2e and integration-style UI coverage gaps

Organize that inventory into:
- Missing entirely
- Present but thin / incomplete
- Explicitly deferred or future UI that should now be brought into scope for Phase 16

Then decide what Phase 16 must include so that, after Phase 16, the product is no longer “backend-first routes with thin surfaces,” but instead has a genuinely comprehensive UI.

Deliverables:
- Create a new spec file at:
  `/Users/polarzero/code/projects/origin/docs/specs/16.comprehensive-ui-integration-and-e2e.spec.md`
  If you find a clearly better slug that matches repo convention, use it, but keep it as Phase 16.
- Create or update any matching Phase 16 log file required by `GUIDE.md`.
- If the phase changes stable direction or scope boundaries, update:
  `/Users/polarzero/code/projects/origin/docs/specs/00.project-direction.md`

The spec you write must match the style of the existing phase specs and include:
- Goal
- Why This Phase Exists
- In Scope
- Out of Scope
- Baseline Assumptions
- Required Keep Set
- Deletion Candidates
- Evidence Anchors
- Agent Execution Plan
- Acceptance Criteria
- Risk Register

Quality bar for the Phase 16 spec:
- It must explicitly cover both UI implementation and testing.
- It must not be a small “polish” phase. It should be the phase that closes the full UI gap.
- It must include all major missing UI, even if earlier phases had deferred them.
- It must be concrete enough that another agent could implement against it without guessing.
- It must include a thorough local e2e plan and validation commands.
- It should define a release-grade UI verification matrix or equivalent acceptance structure for the new surfaces.

The intended end state after Phase 16 should include:
- First-class shell navigation for workflows, library, history, runs, and sessions
- A full workflow operating loop in the UI: discover, author/edit, validate, run, rerun, inspect, continue
- Rich run-detail drill-downs using reused chat/diff/terminal surfaces where appropriate
- Clear session-role semantics
- Comprehensive history/debug/notification/deep-link integration
- Strong empty/loading/error/blocked states
- Accessibility and keyboard-navigation expectations
- Thorough e2e coverage for the whole UI matrix

Do not implement product code in this task unless it is required by the spec workflow itself. The main job is to audit, decide scope, and write the Phase 16 spec and any required companion log/direction updates.

When you finish:
- Summarize the full missing-UI inventory you found
- Summarize the Phase 16 spec you wrote
- Call out any remaining questions you need answered from me
