# 12 Spec: Debug/Reconciliation Visibility and Reporting Flow

## Goal

Implement the hidden-by-default debug/reconciliation UX, long-running notifications, hard-stop behavior, deep-linking, and an optional send-to-developers reporting flow that uses the generic Draft/Dispatcher contract from Phase `10`.

## Why This Phase Exists

Reconciliation is intentionally abstracted for non-technical users, but the system still needs reliable observability and intervention controls when recovery runs long.

## In Scope

- Debug/reconciliation visibility controls:
  - hidden by default
  - reveal via `Show Debug Sessions`
  - compact hidden-count indicator
- Long-running notification behavior:
  - threshold trigger
  - repeat cadence
  - reminder actions (`Open debug session`, `Keep running`, `Stop and report`)
  - remaining-time text to hard stop
- Hard-stop behavior:
  - elapsed-time timeout
  - deterministic failure code
- `Open debug session` deep-link behavior:
  - auto-enable debug visibility in view
  - focus target run/session
- Stop/report flow:
  - field-level preview before consent
  - metadata default
  - allowlisted fields and targets
  - explicit reporting action model aligned with Phase `10` Draft envelope (`source_kind=system_report` + `adapter_id` + `action_id` + `target` + `payload_json`)

## Out of Scope

- New reconciliation algorithm logic.
- Sandboxed execution.
- External crash-reporting infrastructure beyond existing integration paths.

## Baseline Assumptions

- Reconciliation runs are categorized as debug runs by runtime.
- Notification primitives and click-handling exist.
- Reporting path uses a backend-owned system adapter/action aligned with the Phase `10` Draft/Dispatcher gate.
- Reporting payload is authored as a Draft envelope, not a raw transport request.
- Report Drafts created by this flow use `source_kind=system_report`.
- Reserve `adapter_id=system` and `action_id=report.dispatch` for this reporting path.
- Report Drafts are first-class Drafts that appear in the main Drafts surfaces with clear system provenance.
- Report dispatch does not depend on any JJ `integration_attempt_id`; it uses the outbound `dispatch_attempt` contract from Phase `10`.
- Settings contract in this phase:
  - threshold default 15 minutes
  - reminder cadence default 10 minutes
  - hard stop default 45 minutes
  - invariant `hard_stop > threshold`

## Required Keep Set

Not applicable.

## Deletion Candidates

Not applicable.

## Evidence Anchors

- Notification surfaces:
  - [context/notification.tsx](/Users/polarzero/code/projects/origin/packages/app/src/context/notification.tsx)
  - [utils/notification-click.ts](/Users/polarzero/code/projects/origin/packages/app/src/utils/notification-click.ts)
- Session/history surfaces:
  - [pages/session.tsx](/Users/polarzero/code/projects/origin/packages/app/src/pages/session.tsx)
  - [components/titlebar-history.ts](/Users/polarzero/code/projects/origin/packages/app/src/components/titlebar-history.ts)
- Draft/history surfaces:
  - [history.tsx](/Users/polarzero/code/projects/origin/packages/app/src/pages/history.tsx)
  - [history-data.ts](/Users/polarzero/code/projects/origin/packages/app/src/pages/history-data.ts)
- Settings surfaces:
  - [settings-general.tsx](/Users/polarzero/code/projects/origin/packages/app/src/components/settings-general.tsx)

## Agent Execution Plan

### Step 1: Implement Debug Visibility Controls

- Add default hidden behavior for debug runs.
- Add `Show Debug Sessions` persistence and in-view toggle behavior.

### Step 2: Implement Long-Running Reminder Engine

- Trigger first reminder at configured threshold.
- Repeat at configured cadence.
- Include remaining-time text to hard stop.
- Cancel pending reminders immediately when run reaches terminal state.

### Step 3: Implement Hard-Stop Outcome

- Enforce timeout transition with deterministic failure code.
- Keep run/session linkage and audit trail intact.

### Step 4: Implement Deep-Link Behavior

- Ensure `Open debug session` action reveals and focuses target item.

### Step 5: Implement Stop/Report UX

- Add field-level preview.
- Require explicit consent.
- Apply allowlisted fields/target rules.
- Create report Drafts using the Phase `10` generic envelope with `source_kind=system_report`, `adapter_id=system`, and `action_id=report.dispatch`.
- Route report sends through outbound `dispatch_attempt` only; no JJ reconciliation attempt is required or reused.
- Surface report Drafts in the main Drafts UI with clear system provenance.
- Enforce backend dispatcher-only delivery with no direct transport bypass.

### Step 6: Test Coverage

- reminder timing and action behavior
- hard-stop state transition
- deep-link reveal/focus
- report-preview consent constraints
- report Draft provenance and no-JJ-attempt dispatch path

## Acceptance Criteria

- `AC-01` Debug runs are hidden by default and revealable via filter/setting.
  - Verification: UI tests for default and toggled states.
  - Pass condition: behavior matches setting and in-view toggle contract.

- `AC-02` Long-running reminders follow configured timing and include remaining-time text.
  - Verification: timer-driven integration tests.
  - Pass condition: first and repeated reminders fire at correct cadence with required actions.

- `AC-03` `Keep running` does not alter hard-stop deadline.
  - Verification: reminder interaction tests inspecting hard-stop timestamp before/after `Keep running`.
  - Pass condition: hard-stop timestamp is unchanged.

- `AC-04` Hard-stop applies deterministically.
  - Verification: simulated long-running debug run.
  - Pass condition: run transitions to timeout failure code at configured hard stop.

- `AC-05` `Open debug session` deep-link auto-reveals and focuses target.
  - Verification: click notification action while debug filter hidden.
  - Pass condition: target run/session is visible and focused.

- `AC-06` Stop/report flow enforces field preview + explicit consent.
  - Verification: report flow e2e with sensitive fields disabled by default.
  - Pass condition: no report dispatch occurs without explicit final consent.

- `AC-07` Non-allowlisted report targets are blocked.
  - Verification: attempt report dispatch to non-allowlisted `target` or unregistered action/target combination.
  - Pass condition: deterministic block reason and no outbound write.

- `AC-08` Metadata-only default payload is enforced.
  - Verification: initiate report without explicit opt-ins.
  - Pass condition: prompt/file fields are excluded by default and the resulting report Draft payload contains metadata-only fields.

- `AC-09` Stop/report creates a first-class system report Draft without JJ attempt coupling.
  - Verification: stop/report e2e plus Draft query/UI assertions.
  - Pass condition: resulting Draft uses `source_kind=system_report`, `adapter_id=system`, and `action_id=report.dispatch`, is visible in main Drafts surfaces with system provenance, and can be approved/sent through outbound `dispatch_attempt` without any JJ `integration_attempt_id`.

- `AC-10` Terminal-state boundary suppresses stale reminders.
  - Verification: run resolves immediately before scheduled reminder fire.
  - Pass condition: reminder is canceled and not shown.

- `AC-11` Validation commands pass.
  - Verification commands:
```bash
bun run --cwd packages/app typecheck
bun run --cwd packages/app test:unit
bun run --cwd packages/app test:e2e:local -- e2e/notifications
bun run --cwd packages/opencode test
```
  - Pass condition: all commands pass.

## Risk Register

- Timer scheduling drift can cause flaky reminder behavior.
- Hidden-by-default debug policy can reduce discoverability if indicators are weak.
- Report flow can accidentally leak fields if allowlist wiring is incomplete.
- Deep-link focus logic can break with list virtualization/pagination.
