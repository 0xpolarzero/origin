/**
 * Origin Incur CLI API
 *
 * Documentation re-export for the shared Origin incur contract.
 *
 * The authoritative contract now lives in app-owned code at
 * `apps/server/src/cli/spec.ts`, which keeps the runtime independent from
 * `docs/` while preserving this stable documentation entrypoint.
 */

export * from '../../apps/server/src/cli/spec.ts'
export { default } from '../../apps/server/src/cli/spec.ts'
